// Turnery operator
// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Ternary_operator


// output = condition ? value1 : value2
let newAge = (Math.random() > 0.6) ? "my age is greater than 60" : "my age is less than 60";

console.log(newAge);
